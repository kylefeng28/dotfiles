" ~/.vimrc
" Note: This is probably not the Vim/Neovim configuration file you are looking for, which are instead
" at ~/.vim/init.vim and ~/.config/nvim/init.vim
" This ~/.vimrc file is just to help support older versions of Vim and IdeaVim

source ~/.vim/settings.vim
source ~/.vim/mappings.vim

